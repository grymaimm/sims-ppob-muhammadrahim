/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com', 'minio.nutech-integrasi.com'],
  },
};

export default nextConfig;
